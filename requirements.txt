# Core dependencies
pgx @ git+https://github.com/sile16/pgx.git@master
turbozero @ git+https://github.com/sile16/turbozero.git@main

# JAX ecosystem
# Mac uses CPU-only JAX (no jax-metal due to serialization issues)
jax>=0.8.1; sys_platform == "darwin"
jaxlib>=0.8.1; sys_platform == "darwin"
jax[cuda12]; sys_platform == "linux"
chex
flax>=0.12.1

# Distributed training
redis
msgpack-numpy
mlflow
psutil
pynvml; sys_platform == "linux"  # GPU monitoring (NVIDIA)

# Utilities
numpy
tqdm
pyyaml
gnubg

# Testing
pytest
prometheus_client
