# Core dependencies
pgx @ git+https://github.com/sile16/pgx.git@master
turbozero @ git+https://github.com/sile16/turbozero.git@main

# JAX ecosystem
jax<0.5.1; sys_platform == "darwin"
jaxlib<0.5.1; sys_platform == "darwin"
jax<0.7; sys_platform == "linux"
jax[cuda12]; sys_platform == "linux"
chex
flax

# Distributed training
ray[default,client]
redis
msgpack-numpy

# Utilities
numpy
tqdm
pyyaml
gnubg

# Testing
pytest
prometheus_client
